import { connect } from 'react-redux'
import Splash from './Splash'

const mSTP = state => {
    return{

    }
}

const mDTP = dispatch => {
    return{

    }
}

export default connect(mSTP, mDTP)(Splash)