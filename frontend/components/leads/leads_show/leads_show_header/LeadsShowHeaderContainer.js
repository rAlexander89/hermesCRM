import { connect } from 'react-redux';
import LeadsShowHeader from './LeadsShowHeader';

const mSTP = state => {
    return{
    }
}

const mDTP = dispatch => {
    return {
    }
}

export default connect(mSTP, mDTP)(LeadsShowHeader);
