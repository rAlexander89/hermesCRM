import { connect } from 'react-redux';
import LeadsShowDetails from './LeadsShowDetails'

const mSTP = state => {
    return{
    }
}

const mDTP = dispatch => {
    return {
    }
}

export default connect(mSTP, mDTP)(LeadsShowDetails);
